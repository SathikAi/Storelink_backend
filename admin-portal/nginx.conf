server {
    listen 80;
    root /usr/share/nginx/html;

    location /admin/ {
        try_files $uri $uri/ /admin/index.html;
    }

    location /health {
        return 200 'ok';
        add_header Content-Type text/plain;
    }

    location / {
        return 301 /admin/;
    }
}
